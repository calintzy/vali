function add(a: number, b: number): number {
  return a + b;
}

function subtract(a: number, b: number): number {
  return a - b;
}

// 복잡한 로직 설명
function complexCalc(values: number[]): number {
  return values.reduce((sum, v) => sum + v, 0);
}
